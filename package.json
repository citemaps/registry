{
  "name": "citemaps-org-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Public registry + index for citemap.json files on the open web. The neutral catalog layer of the citemap spec — separate from any single AEO tool. Phase 1 ships the submission API + validation worker; later phases add public index pages, auto-discovery, verification + claim, and Common Crawl mining.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "next lint"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "next": "16.0.10",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
